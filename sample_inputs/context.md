*User Managemet APIs*
Its a User management module that exposes 5 APIs, like add user, delete user, get all user or get user by id, and edit user.

These APIs are used by diferent modules, to manage users in their aplications.
It uses postgres as a DB to mantain user's data.
